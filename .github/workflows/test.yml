name: test
on:
  push:
    branches:
    - '*'

jobs:
  monorepo-generator-test:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: sudo apt-get install -y build-essential opam sudo pkg-config neovim autoconf curl

      - run: ls
